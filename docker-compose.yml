services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./mlruns.db:/app/mlruns.db
      - ./models:/app/models
    environment:
      - MLFLOW_TRACKING_URI=sqlite:////app/mlruns.db
